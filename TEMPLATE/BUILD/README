This directory is propose to make the executable.

#FIRST
cp ../variables_template.cmake ../variables.cmake
#and modify it according the local environment

#to build the make
cmake ..

#to run the tests
make debug/release/native

#to completely reset cmake (changing compiler, ...), in BUILD do
rm -rf *
#!!! THIS WILL DELETE THIS README